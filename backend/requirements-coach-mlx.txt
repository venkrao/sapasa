# Optional: real Qwen2.5-Omni inference on Apple Silicon (M1/M2/M3/M4).
# Install in the same venv as the pitch server, then set SAPASA_COACH_MLX_MODEL.
#
#   pip install -r requirements-coach-mlx.txt
#
# Example model (3B 4-bit, fits most M-series machines):
#   export SAPASA_COACH_MLX_MODEL=giangndm/qwen2.5-omni-3b-mlx-4bit
#
# Upstream: https://github.com/giangndm/mlx-lm-omni
#
# If you see ImportError for get_model_path from mlx_lm.utils, upgrade pip/mlx-lm;
# SaPaSa patches get_model_path, mlx.nn.quantize (legacy ExtendedEmbedding.to_quantized),
# and TokenizerWithAudio.get_vocab delegation in coach_mlx.py for current mlx / mlx-lm releases.

git+https://github.com/giangndm/mlx-lm-omni.git
